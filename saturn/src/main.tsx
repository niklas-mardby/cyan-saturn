import React from "react";
import ReactDOM from "react-dom/client";
import SaturnApp from "./SaturnApp.tsx";
import "./index.scss";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <SaturnApp />
  </React.StrictMode>
);
